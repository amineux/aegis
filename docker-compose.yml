name: aegis
services:
  aegis:
    # Builds locally from the Dockerfile so you always run this clone.
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aegis
    # Host port is configurable via AEGIS_PORT in .env (defaults to 3000).
    ports:
      - "${AEGIS_PORT:-3000}:3000"
    # Optional API keys / scanner backend — copy .env.example to .env and
    # fill in what you need. Missing file is ignored (keyless feeds still work).
    env_file:
      - path: .env
        required: false
    environment:
      - NODE_OPTIONS=--dns-result-order=ipv4first
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
    networks:
      - default

  aegis-cache:
    image: nginx:alpine
    container_name: aegis-cache
    ports:
      - "8080:8080"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - nginx-cache:/var/cache/nginx
    depends_on:
      - aegis
    restart: unless-stopped
    networks:
      - default

  aegis-intel:
    build:
      context: ./intel
      dockerfile: Dockerfile
    container_name: aegis-intel
    ports:
      - "4000:4000"
    restart: unless-stopped
    networks:
      - default

volumes:
  nginx-cache:

# ── CasaOS app-store metadata (ignored by plain Docker Compose) ──
x-casaos:
  architectures:
    - amd64
    - arm64
  main: aegis
  author: amineux
  developer: amineux
  category: Network
  description:
    en_us: >-
      Aegis is amineux’s personal OSINT command center. It aggregates
      real-time global intelligence across aviation, maritime, CCTV, seismic
      activity, wildfires, news, weather, space weather, cyber threats and
      conflict zones on a GPU-accelerated MapLibre map. Core feeds work with
      no API keys.
  tagline:
    en_us: Personal OSINT command center
  title:
    en_us: Aegis
  icon: https://raw.githubusercontent.com/amineux/aegis/master/public/casaos-icon.png
  thumbnail: https://raw.githubusercontent.com/amineux/aegis/master/public/og-image.png
  screenshot_link:
    - https://raw.githubusercontent.com/amineux/aegis/master/public/og-image.png
  scheme: http
  hostname: ""
  port_map: "3000"
  index: /
  webui_port: 3000
  envs:
    - container: SCANNER_URL
      description:
        en_us: "(Optional) RECON scanner backend URL, e.g. http://scanner:7700"
    - container: SCANNER_KEY
      description:
        en_us: "(Optional) RECON scanner shared secret (must match the backend scanner key)."
    - container: FIRMS_API_KEY
      description:
        en_us: "(Optional) NASA FIRMS API key for active-fire data."
    - container: AIS_API_KEY
      description:
        en_us: "(Optional) aisstream.io key for maritime vessel tracking."
    - container: OPENSKY_CLIENT_ID
      description:
        en_us: "(Optional) OpenSky OAuth2 client id for higher aircraft rate limits."
    - container: OPENSKY_CLIENT_SECRET
      description:
        en_us: "(Optional) OpenSky OAuth2 client secret."
    - container: N2YO_API_KEY
      description:
        en_us: "(Optional) N2YO key for satellite tracking."
  ports:
    - container: "3000"
      description:
        en_us: "Web UI (HTTP)."
